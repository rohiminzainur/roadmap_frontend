// Operator
// Aritmatika (kukabataku)
//  (*, /, -, +, %)

// Penugasan
// (=, +=, -=, *=, /=, %=)

// Perbandingan
// (==, !=, ===, !==, >, <, >=, <=)

// Logika
// &&, ||, !

// String
// +

// Kondisional
// (x % 2 == 0) ? "genap" : "ganjil";

// Typeof
// typeof(10)
// number